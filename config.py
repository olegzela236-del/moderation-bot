"""
Конфигурация бота-модератора.

Токен и ID группы читаются из переменных окружения (.env),
остальные параметры (пороги антиспама, ключевые слова и т.д.)
можно менять прямо здесь.
"""
import os

from dotenv import load_dotenv

load_dotenv()

# --- Обязательные параметры ---
BOT_TOKEN = os.getenv("BOT_TOKEN", "")
# ID группы, которую модерирует бот (например, -1001234567890)
GROUP_ID = int(os.getenv("GROUP_ID", "0"))

# --- Файлы ---
DB_PATH = "moderation.db"
BAD_WORDS_FILE = "bad_words.txt"
LOG_FILE = "bot.log"

# --- Антиспам ---
DUPLICATE_WINDOW_SECONDS = 30   # окно проверки повторяющихся сообщений
DUPLICATE_THRESHOLD = 2         # сколько одинаковых сообщений подряд считать спамом
EMOJI_LIMIT = 10                # максимум эмодзи в одном сообщении

FLOOD_MESSAGES = 5              # сколько сообщений...
FLOOD_WINDOW_SECONDS = 5        # ...за какое время считается флудом
FLOOD_MUTE_MINUTES = 10         # на сколько минут мутить за флуд

# --- Антиреклама ---
# Ключевые слова-триггеры рекламы (регистр не важен, ищутся как подстрока)
AD_KEYWORDS = ["куплю", "продам", "заработок", "криптовалют", "реклама"]

# --- Система предупреждений ---
WARN_RESET_HOURS = 24           # через сколько часов сбрасывается счётчик предупреждений
MUTE_ON_SECOND_WARN_HOURS = 1   # длительность мута при 2-м предупреждении
WARNING_MESSAGE_TTL = 5         # сколько секунд висит уведомление о нарушении в чате

# --- Капча для новых участников ---
CAPTCHA_TIMEOUT_SECONDS = 60
